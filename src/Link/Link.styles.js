import styled from 'styled-components'

const Link = styled.a`
  text-decoration: none;

  &:link,
  &:visited {
    font-size: inherit;
    font-weight: inherit;
    color: inherit;
  }
`

export { Link }
